// apps/web/app/imoveis/components/SearchFilters.tsx
// MÓDULO 3: Candidato deve implementar este componente + testes
"use client";

/**
 * CANDIDATO: Implemente este componente.
 *
 * Requisitos:
 * 1. Filtros: bairro (multi-select), faixa de preço (min/max), suítes (1-5+), metragem mínima
 * 2. Cada filtro atualiza os searchParams da URL (sem page reload)
 * 3. URL é source of truth — abrir URL com filtros deve restaurá-los
 * 4. Funciona sem JavaScript no primeiro render (searchParams do Server Component)
 * 5. Botão "Limpar filtros" reseta tudo
 * 6. Validação: preço mínimo > máximo mostra erro
 *
 * Bairros disponíveis: Jardins, Itaim Bibi, Vila Nova Conceição, Moema, Pinheiros, Brooklin
 */

export function SearchFilters() {
  // TODO: implementar
  return <div>SearchFilters — não implementado</div>;
}
