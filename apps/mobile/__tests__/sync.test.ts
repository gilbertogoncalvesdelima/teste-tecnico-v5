// apps/mobile/__tests__/sync.test.ts

/**
 * CANDIDATO: Escreva no mínimo 4 testes:
 *
 * 1. Enfileira e processa operação com sucesso → status DONE
 * 2. Retry com backoff após falha do executor
 * 3. Não duplica operação com mesma entityId + type (idempotência)
 * 4. Marca como FAILED após maxRetries tentativas
 *
 * Dica: Use vi.fn() para criar executors que falham ou sucedem conforme o teste.
 * Dica: Use vi.useFakeTimers() para testar o backoff sem esperar segundos reais.
 */

describe.todo("useOfflineQueue — candidato implementa");
