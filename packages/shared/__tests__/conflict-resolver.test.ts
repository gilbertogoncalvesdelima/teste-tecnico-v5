// packages/shared/__tests__/conflict-resolver.test.ts

/**
 * CANDIDATO: Escreva no mínimo 5 testes, um para cada regra de negócio:
 *
 * 1. Apenas status mudou → SERVER_WINS
 * 2. Apenas notes/photos mudaram → LOCAL_WINS
 * 3. Price mudou nos dois lados → SERVER_WINS
 * 4. Campos diferentes mudaram em cada lado → MERGED
 * 5. Mesmo campo (não status/price) mudou nos dois → LOCAL_WINS + requiresReview
 *
 * Dica: crie um helper makeProperty() para facilitar a construção de fixtures.
 */

describe.todo("SyncConflictResolver — candidato implementa");
